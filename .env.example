# Airi Desktop Pet - 配置文件
#
# 复制为 .env 或直接设置系统环境变量

# Python 解释器路径（留空则自动检测系统 PATH 中的 python）
AIRI_PYTHON_PATH=

# DeepSeek API Key（可选，用于 AI 生成回复，不留空使用本地语料库）
DEEPSEEK_API_KEY=

# 角色立绘图片路径（可选，默认自动检测 assets/ 目录）
AIRI_CHARACTER_IMAGE=

# 桌面宠物 SSE 服务器端口（可选，默认 19876）
AIRI_STANDALONE_PORT=19876
