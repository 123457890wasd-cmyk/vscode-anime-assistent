# Cubism Core 是 Live2D 专有运行时，不可再分发 —— 别提交
vendor/live2dcubismcore.min.js

# 模型是 CC BY-NC-SA 4.0 且体积较大，按需再决定是否入库
model/

# 运行产物
report.json
report_webview.json
shot.png
shot_webview.png
hits.log
trace.jsonl
.done
*_result.txt
result_*.txt
launcher_check.txt
matrix_result.txt
bisect_result.txt

# 透明探针对照产物（可重跑 probe_transparency.py 重建，结论留在 transparency_result.txt）
tp_*
tpw_*
shot_pixels.txt

# 镂空 / 点击验证的截图与回报（可重跑重建）
pet_ui_shot.png
region_*.png
silhouette_report.json
.pet_ui_done
.silhouette_done

# 活窗口取证产物（第三轮）：都是可重跑重建的原始数据/截图。
# 注意 live_fix*.txt 是"DWM Mica 是真根因"的关键证据，**要提交**，
# 所以这里只忽略图片和派生文件，不用 live_* 这种通配。
live_top.png
live_kid*.png
alpha_visible*.png
alpha_hidden.png
fix2_*.png
fix3_*.png
fix4_*.png
verify_fix.png
webview_loc_*.txt
bridge_probe_*.txt
shot_pixels_*.txt

# 我自己的临时/中间文件（下划线开头）
_*.txt

# 用户机器上跑出来的原始日志备份（含本机路径与 pid）
log_user_run_*.txt
live_alive.txt
