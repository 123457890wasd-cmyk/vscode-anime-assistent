{
  "name": "vscode-anime-assistent",
  "displayName": "airi-monitor",
  "description": "Anime desktop pet for VS Code — tsundere Airi watches your code and roasts your bugs",
  "version": "0.2.1",
  "engines": {
    "vscode": "^1.110.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-anime-assistent.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "vscode-anime-assistent.openAssistant",
        "title": "Open Anime Assistant"
      },
      {
        "command": "vscode-anime-assistent.launchPet",
        "title": "Launch Airi Desktop Pet"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.110.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.56.1",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
